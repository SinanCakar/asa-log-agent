# PyInstaller / build outputs
build/
dist/
*.spec

# Python
__pycache__/
*.pyc

# Runtime / local state (never commit — agent.ini may hold your token)
offline_queue.jsonl
calibration_screenshot.png
tests/synth_log.png

# Keep agent.ini tracked as a template ONLY if its token field is empty.
# If you fill in a real token locally, do NOT commit it.

# Installer build artifacts
installer/vendor/
installer/output/

# Build-time version (generated by CI)
_version.py
